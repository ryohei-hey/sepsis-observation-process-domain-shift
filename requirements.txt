# Pinned to the exact versions used to produce the results reported in the
# manuscript and its Supporting Information (analysis re-run 2026-07-28).
# Python 3.14.2

# --- core analysis
pandas==3.0.5
numpy==2.5.1
scipy==1.18.0
scikit-learn==1.9.0
xgboost==3.3.0
statsmodels==0.14.6          # unpenalized calibration slope; offset-model CITL
pyarrow==25.0.0              # parquet engine for the wide feature frames

# --- figures
matplotlib==3.11.1
seaborn==0.13.2

# --- tables and reporting
python-docx==1.2.0
tableone==0.9.6

# --- data access (cohort extraction only; requires PhysioNet credentialing)
google-cloud-bigquery==3.42.2
